/**
 *  What is "Named Function Expression" ?
 *  "Named Function Expression" is kind of similar anonymous function but here we named to our function
 */

const carName = function name() {
    console.log("I am car name");
    
}

carName();

// name();  This will throw an error.



/**
 *  To call "name function expression" we need to call the variable name which is assigned to function. If we call the function name it wil throw an error.
 */
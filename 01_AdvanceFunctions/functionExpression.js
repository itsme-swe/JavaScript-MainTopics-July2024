/**
 *  What is Function Expression ?
 *  Function expression is just passing function as value to a variable.
 */



let b = function b() {
    console.log("Function b called!!");
    
}

b();

/**
 * 🚩 The main difference b/w "Function Statement and Function Expression" is hoisting.
 *      We can call "Function Statement" before creating it but we cannot call "Function Expression" before creating it.
 */

